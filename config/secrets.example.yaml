# =============================================================================
# Arabesque - Credentials (NE PAS COMMITER)
# =============================================================================
# Copier vers config/secrets.yaml et remplir avec vos vraies valeurs.
# Ce fichier est dans .gitignore.
# =============================================================================

# -----------------------------------------------------------------------------
# cTrader - FTMO Challenge
# -----------------------------------------------------------------------------
ftmo_ctrader:
  client_id: "YOUR_CLIENT_ID"
  client_secret: "YOUR_CLIENT_SECRET"
  access_token: "YOUR_ACCESS_TOKEN"
  refresh_token: "YOUR_REFRESH_TOKEN"
  account_id: null             # null = auto-détection via API

# -----------------------------------------------------------------------------
# cTrader - FTMO compte test
# -----------------------------------------------------------------------------
ftmo_ctrader_test:
  client_id: "YOUR_CLIENT_ID"
  client_secret: "YOUR_CLIENT_SECRET"
  access_token: "YOUR_ACCESS_TOKEN"
  refresh_token: "YOUR_REFRESH_TOKEN"
  account_id: null

# -----------------------------------------------------------------------------
# TradeLocker - GFT Compte 1
# -----------------------------------------------------------------------------
gft_compte1:
  email: "your@email.com"
  password: "YOUR_PASSWORD"
  account_id: null             # null = premier compte actif

# -----------------------------------------------------------------------------
# TradeLocker - GFT Compte 2 (décommenter si nécessaire)
# -----------------------------------------------------------------------------
# gft_compte2:
#   email: "your@email.com"
#   password: "YOUR_PASSWORD"
#   account_id: null

# -----------------------------------------------------------------------------
# Notifications (channels avec tokens)
# -----------------------------------------------------------------------------
# Les URLs de notification contiennent des tokens — elles vont ici, pas dans
# settings.yaml. load_full_config() les fusionne automatiquement.
#
# Formats supportés (bibliothèque apprise) :
#   Telegram : tgram://BOT_TOKEN/CHAT_ID
#   ntfy     : ntfy://ntfy.sh/TOPIC
#   Discord  : discord://WEBHOOK_ID/WEBHOOK_TOKEN
#   Slack    : slack://TOKEN_A/TOKEN_B/TOKEN_C/CHANNEL
# -----------------------------------------------------------------------------
notifications:
  channels: []
  # channels:
  #   - "tgram://1234567890:ABCDEF.../987654321"
  #   - "ntfy://ntfy.sh/arabesque-alerts"
